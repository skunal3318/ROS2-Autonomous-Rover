<launch>

  <!-- ===================== -->
  <!-- GLOBAL VARIABLES      -->
  <!-- ===================== -->
  <let name="use_sim_time" value="true"/>

  <let name="urdf_path"
       value="$(find-pkg-share autonomous_rover_description)/urdf/rover.urdf.xacro" />

  <let name="gazebo_config_path"
       value="$(find-pkg-share rover_bringup)/config/gazebo_bridge.yaml" />

  <let name="rviz_config_path"
       value="$(find-pkg-share autonomous_rover_description)/rviz/urdf_config.rviz" />

  <!-- ===================== -->
  <!-- ROBOT STATE PUBLISHER -->
  <!-- ===================== -->
  <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
    <param name="robot_description"
           value="$(command 'xacro $(var urdf_path)')" />
  </node>

  <!-- ===================== -->
  <!-- GAZEBO SIM            -->
  <!-- ===================== -->
  <include file="$(find-pkg-share ros_gz_sim)/launch/gz_sim.launch.py">
    <arg name="gz_args" value="empty.sdf -r"/>
  </include>

  <!-- ===================== -->
  <!-- SPAWN ROBOT           -->
  <!-- ===================== -->
  <node pkg="ros_gz_sim" exec="create"
        args="-topic robot_description"/>

  <!-- ===================== -->
  <!-- GAZEBO ↔ ROS BRIDGE   -->
  <!-- ===================== -->
  <node pkg="ros_gz_bridge" exec="parameter_bridge">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
    <param name="config_file"
           value="$(var gazebo_config_path)" />
  </node>

  <!-- ===================== -->
  <!-- RVIZ                 -->
  <!-- ===================== -->
  <node pkg="rviz2" exec="rviz2"
        args="-d $(var rviz_config_path)">
    <param name="use_sim_time" value="$(var use_sim_time)"/>
  </node>

</launch>
